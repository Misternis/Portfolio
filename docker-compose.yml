version: '3.9'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile
    ports:
      - "80:5000"
    volumes:
      - .:/app
    working_dir: /app
    command: python run.py
    restart: always
